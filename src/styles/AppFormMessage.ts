import { unstable_FormMessage as FormMessage } from 'reakit'
import styled from 'styled-components';

export const AppFormMessage = styled(FormMessage)`
  color: #F23;
  font-size: 0.8rem;
  font-weight: bold;
  margin: 0.2rem 0 0.5rem 0;
`;